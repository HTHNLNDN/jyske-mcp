apscheduler>=3.10.0
fastapi>=0.115.0
uvicorn>=0.32.0
litellm>=1.83.0
langfuse>=2.60.0,<3
pydantic>=2.13.0  # langfuse 2.x's bundled pydantic.v1 shim needs this on py3.14 (see src/jyske_mcp/llm.py); litellm pins ==2.12.5 but works fine with 2.13.x in practice
itsdangerous>=2.2.0
alembic>=1.18.0
sqlalchemy>=2.0.0
pytest>=8.0   # test suite; run with make test
httpx>=0.27.0  # dev/test: FastAPI TestClient (starlette) transport
cryptography==49.0.0  # RS256 backend for PyJWT (Enable Banking auth)
PyJWT==2.13.0
python-dotenv==1.2.2
requests==2.34.2
